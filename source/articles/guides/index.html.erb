<ul class="sk-grid">
  <% blog.tags.keys.each do |tag| %>
    <%= partial 'partials/grid_item', locals: {
      title: tag.to_title_case.to_human_case,
      description: 'Examples & Guides',
      href: "/articles/guides/#{tag}",
      icon: nil
    } %>
  <% end %>
</ul>
